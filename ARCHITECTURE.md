# Calendar Embed Architecture

## 🏗️ System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                        USER INTERFACES                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────┐      ┌──────────────┐      ┌──────────────┐  │
│  │ Mobile App   │      │ WordPress    │      │ React Native │  │
│  │ (Native)     │      │ Embed        │      │ Web Embed    │  │
│  │              │      │ (iframe)     │      │ (Full App)   │  │
│  └──────┬───────┘      └──────┬───────┘      └──────┬───────┘  │
│         │                     │                      │          │
│         │ tRPC Client         │ fetch/tRPC          │ tRPC     │
│         │                     │                      │          │
└─────────┼─────────────────────┼──────────────────────┼──────────┘
          │                     │                      │
          └─────────────────────┼──────────────────────┘
                                │
┌───────────────────────────────▼──────────────────────────────────┐
│                        BACKEND API LAYER                          │
├───────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Hono Server (Node.js)                                   │   │
│  │  URL: /api/trpc/*                                        │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  tRPC Router (app-router.ts)                             │   │
│  │                                                           │   │
│  │  ├─ calendar.getBookings    [Query]                      │   │
│  │  ├─ calendar.setBooking     [Mutation]                   │   │
│  │  ├─ calendar.updateHoldDuration [Mutation]               │   │
│  │  ├─ calendar.getEvents      [Query]                      │   │
│  │  ├─ calendar.addEvent       [Mutation]                   │   │
│  │  ├─ calendar.updateEvent    [Mutation]                   │   │
│  │  └─ calendar.deleteEvent    [Mutation]                   │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
└───────────────────────────────────────────────────────────────────┘
                                │
                                │
┌───────────────────────────────▼──────────────────────────────────┐
│                        DATA STORAGE LAYER                         │
├───────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌────────────────────┐         ┌────────────────────┐           │
│  │  Bookings Store    │         │  Events Store      │           │
│  │  (In-Memory)       │         │  (In-Memory)       │           │
│  │                    │         │                    │           │
│  │  Record<string,    │         │  Record<string,    │           │
│  │    DateBooking>    │         │    VenueEvent>     │           │
│  │                    │         │                    │           │
│  │  ⚠️  Resets on     │         │  ⚠️  Resets on     │           │
│  │     server         │         │     server         │           │
│  │     restart        │         │     restart        │           │
│  └────────────────────┘         └────────────────────┘           │
│                                                                   │
└───────────────────────────────────────────────────────────────────┘
```

## 📊 Data Flow Diagram

### 1. Setting a Booking (Mobile App → Backend)

```
User Taps Date
     │
     ▼
┌─────────────────┐
│ VenueContext    │
│ setDateStatus() │
└────────┬────────┘
         │
         │ Create DateBooking object
         ▼
┌─────────────────────────┐
│ trpc.calendar           │
│   .setBooking           │
│   .mutate()             │
└────────┬────────────────┘
         │
         │ HTTP POST
         ▼
┌─────────────────────────┐
│ Backend API             │
│ setBookingProcedure     │
└────────┬────────────────┘
         │
         │ Store in memory
         ▼
┌─────────────────────────┐
│ bookingsStore           │
│ { [date]: booking }     │
└─────────────────────────┘
```

### 2. Displaying Calendar (Embed → Backend)

```
WordPress Page Loads
     │
     ▼
┌─────────────────┐
│ calendar-embed  │
│   .html         │
└────────┬────────┘
         │
         │ Every 30s
         ▼
┌─────────────────────────┐
│ fetch() API call        │
│ /api/trpc/calendar      │
│   .getBookings          │
└────────┬────────────────┘
         │
         │ HTTP GET
         ▼
┌─────────────────────────┐
│ Backend API             │
│ getBookingsProcedure    │
└────────┬────────────────┘
         │
         │ Read from memory
         ▼
┌─────────────────────────┐
│ bookingsStore           │
│ Filter expired holds    │
└────────┬────────────────┘
         │
         │ Return active bookings
         ▼
┌─────────────────────────┐
│ calendar-embed.html     │
│ Render calendar         │
└─────────────────────────┘
```

## 🔄 Synchronization Flow

```
Time: 00:00
┌���─────────┐         ┌──────────┐         ┌──────────┐
│ Mobile   │         │ Backend  │         │ Embed    │
│ App      │         │ Server   │         │ Calendar │
└────┬─────┘         └────┬─────┘         └────┬─────┘
     │                    │                     │
     │ Set Booking        │                     │
     │ "2025-01-15"       │                     │
     │───────────────────>│                     │
     │                    │                     │
     │                    │ Store in memory     │
     │                    │ ✓                   │
     │<───────────────────│                     │
     │ Success            │                     │
     │                    │                     │
     │                    │                     │
     
Time: 00:15 (15 seconds later)
     │                    │                     │
     │                    │                     │
     │                    │ Embed polls API     │
     │                    │<────────────────────│
     │                    │                     │
     │                    │ Return bookings     │
     │                    │ (includes new one)  │
     │                    │─────────────────────>
     │                    │                     │
     │                    │                     │ Update
     │                    │                     │ Display
     │                    │                     │ ✓
```

## 📁 File Structure

```
project/
├── app/
│   ├── (tabs)/
│   │   └── availability.tsx          # Main booking interface
│   ├── embed/
│   │   └── calendar.tsx              # React Native Web embed
│   └── _layout.tsx                   # Provider setup
│
├── backend/
│   ├── hono.ts                       # Server entry point
│   └── trpc/
│       ├── app-router.ts             # Main router config
│       ├── create-context.ts         # tRPC context
│       └── routes/
│           └── calendar/
│               ├── bookings/
│               │   └── route.ts      # Booking procedures
│               └── events/
│                   └── route.ts      # Event procedures
│
├── contexts/
│   ├── VenueContext.tsx              # Booking state + sync
│   └── EventContext.tsx              # Event state + sync
│
├── public/
│   └── calendar-embed.html           # Static HTML embed
│
├── lib/
│   └── trpc.ts                       # tRPC client config
│
└── docs/
    ├── QUICK_START_WORDPRESS.md      # Quick setup
    ├── WORDPRESS_EMBED.md            # Full documentation
    ├── IMPLEMENTATION_SUMMARY.md     # What was built
    ├── ARCHITECTURE.md               # This file
    └── TEST_EMBED.html               # Test page
```

## 🔐 Security Model

```
┌─────────────────────────────────────┐
│  PUBLIC ACCESS (No Auth)            │
├─────────────────────────────────────┤
│  ✓ View calendar embed              │
│  ✓ Query bookings (GET)             │
│  ✓ Query events (GET)               │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  APP ACCESS (Via Mobile App)        │
├─────────────────────────────────────┤
│  ✓ Set bookings (POST)              │
│  ✓ Add/Update/Delete events (POST)  │
│  ✓ Update hold duration (POST)      │
└─────────────────────────────────────┘

Note: Currently no authentication enforced
      All mutations are public procedures
      Consider adding auth in production
```

## ⚡ Performance Characteristics

| Metric | Value | Notes |
|--------|-------|-------|
| **Sync Latency** | 0-30s | Depends on poll timing |
| **API Response** | <100ms | In-memory read |
| **Auto-refresh** | 30s | Configurable |
| **Storage** | Memory | Volatile, fast |
| **Concurrent Users** | High | Stateless reads |

## 🔮 Future Enhancements

### Phase 1: Persistence
```
┌─────────────┐
│  In-Memory  │ ───> Replace with ───> ┌──────────┐
│   Storage   │                        │ Database │
└─────────────┘                        │ Postgres │
                                       └──────────┘
```

### Phase 2: Real-time Sync
```
┌─────────┐                ┌──────────┐
│ Polling │ ───> Replace   │ WebSocket│
│  30s    │      with      │ Real-time│
└─────────┘                └──────────┘
```

### Phase 3: Multi-tenancy
```
Single Store ───> Multiple Stores
    │                      │
    ▼                      ▼
bookingsStore      tenantBookings[userId]
```

## 🎯 Key Design Decisions

1. **In-memory Storage**
   - ✅ Fast performance
   - ✅ Simple implementation
   - ❌ Data loss on restart
   - 💡 Good for MVP/testing

2. **Polling vs WebSocket**
   - ✅ Simple to implement
   - ✅ Works everywhere
   - ❌ 30s latency
   - 💡 Upgrade to WS later

3. **Public API**
   - ✅ Easy embed setup
   - ✅ No CORS issues
   - ❌ No access control
   - 💡 Add auth when needed

4. **tRPC**
   - ✅ Type-safe API
   - ✅ Auto-generated client
   - ✅ React Query integration
   - ✅ Great DX

## 📝 Notes

- Backend uses **public procedures** for easy embedding
- Data syncs **automatically** every 30 seconds
- **In-memory** storage means data is lost on server restart
- Consider adding **database** for production use
- All **CRUD operations** available via tRPC API
- **No authentication** currently required for embed
